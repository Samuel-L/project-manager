/*
 * This file (Login) is used in the entry point for the homepage scene.
 *
 * Login gets loaded when clicking on the 'Login' link in the
 * navigation bar.
 *
 * The route to this is '/login' and does not require you to be
 * logged in.
*/

import React from 'react';

export default class Login extends React.Component {
  render() {
    return (
      <div className="login-container">
        TestLogin
      </div>
    );
  }
}
